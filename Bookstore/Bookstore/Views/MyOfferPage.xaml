<UserControl x:Class="Bookstore.Views.MyOfferPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Bookstore.Views"
             xmlns:viewModels="clr-namespace:Bookstore.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0 0 0 20">
            <TextBlock Text="Moje oferty" Style="{StaticResource MaterialDesignHeadline5TextBlock}" />
            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding AddOfferCommand}"
                    Margin="20 0 0 0">
                <materialDesign:PackIcon Kind="Plus" />
            </Button>
        </StackPanel>

        <!-- Zakładki -->
        <TabControl Grid.Row="1">
            <TabItem Header="Wersja robocza">
                <ListBox ItemsSource="{Binding DraftOffers}" DisplayMemberPath="Title"/>
            </TabItem>
            <TabItem Header="Publiczne">
                <ListBox ItemsSource="{Binding PublicOffers}" DisplayMemberPath="Title"/>
            </TabItem>
            <TabItem Header="Prywatne">
                <ListBox ItemsSource="{Binding PrivateOffers}" DisplayMemberPath="Title"/>
            </TabItem>
            <TabItem Header="Zrealizowane">
                <ListBox ItemsSource="{Binding OrderedOffers}" DisplayMemberPath="Title"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
