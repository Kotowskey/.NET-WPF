<UserControl x:Class="Bookstore.Views.OrdersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Style="{StaticResource MaterialDesignHeadline5TextBlock}" 
                   Text="Zamówienia" 
                   Margin="0,0,0,16"/>

        <!-- Search and Controls -->
        <materialDesign:Card Grid.Row="1" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="SearchBox"
                         Grid.Column="0"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Szukaj zamówień..."
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         VerticalAlignment="Center"/>

                <Button Grid.Column="1"
                        Style="{StaticResource MaterialDesignIconButton}"
                        Command="{Binding RefreshCommand}"
                        Margin="8,0,0,0"
                        ToolTip="Odśwież">
                    <materialDesign:PackIcon Kind="Refresh"/>
                </Button>
            </Grid>
        </materialDesign:Card>

        <!-- Content -->
        <Grid Grid.Row="2">
            <!-- Loading -->
            <materialDesign:Card Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                                 VerticalAlignment="Center" 
                                 HorizontalAlignment="Center"
                                 Padding="32">
                <StackPanel Orientation="Horizontal">
                    <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                                 IsIndeterminate="True"
                                 Width="24" Height="24"
                                 Margin="0,0,16,0"/>
                    <TextBlock Text="Ładowanie zamówień..." 
                               VerticalAlignment="Center"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Orders List -->
            <materialDesign:Card Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverted}">
                <DataGrid x:Name="OrdersDataGrid"
                          ItemsSource="{Binding Orders}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          CanUserResizeColumns="True"
                          CanUserSortColumns="True"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" 
                                            Binding="{Binding Id}"
                                            Width="60"/>
                        <DataGridTextColumn Header="Klient" 
                                            Binding="{Binding CustomerName}"
                                            Width="150"/>
                        <DataGridTextColumn Header="Książka" 
                                            Binding="{Binding BookTitle}"
                                            Width="200"/>
                        <DataGridTextColumn Header="Data zamówienia" 
                                            Binding="{Binding OrderDateDisplay}"
                                            Width="120"/>
                        <DataGridTextColumn Header="Cena" 
                                            Binding="{Binding PriceDisplay}"
                                            Width="80"/>
                        <DataGridTextColumn Header="Status" 
                                            Binding="{Binding Status}"
                                            Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </materialDesign:Card>

            <!-- No Results -->
            <TextBlock Visibility="{Binding NoResults, Converter={StaticResource BooleanToVisibilityConverter}}"
                       Style="{StaticResource MaterialDesignBody1TextBlock}"
                       Text="Brak zamówień do wyświetlenia"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"/>
        </Grid>
    </Grid>
</UserControl>
